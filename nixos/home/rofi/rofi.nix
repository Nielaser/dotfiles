{
  programs.rofi = {
    enable = true;
    theme = "sidebar";
    font = "DepartureMono Nerd Font Mono";
    modes = [ "drun" "run" "window" "ssh" ];
    extraConfig = {
      show-icons = true;
  };

  };
}
